<form method="post" action="{{ path('water_transport_delete', {'id': water_transport.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ water_transport.id) }}">
    <button class="btn">Delete</button>
</form>
