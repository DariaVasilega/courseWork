{% extends 'base.html.twig' %}

{% block title %}MaritimeTransport{% endblock %}

{% block body %}
    <h1>MaritimeTransport</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ maritime_transport.id }}</td>
            </tr>
            <tr>
                <th>Engine</th>
                <td>{{ maritime_transport.engine }}</td>
            </tr>
            <tr>
                <th>Seats</th>
                <td>{{ maritime_transport.seats }}</td>
            </tr>
            <tr>
                <th>Weight</th>
                <td>{{ maritime_transport.weight }}</td>
            </tr>
            <tr>
                <th>Size</th>
                <td>{{ maritime_transport.size }}</td>
            </tr>
            <tr>
                <th>FuelCost</th>
                <td>{{ maritime_transport.fuelCost }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('maritime_transport_index') }}">back to list</a>

    <a href="{{ path('maritime_transport_edit', {'id': maritime_transport.id}) }}">edit</a>

    {{ include('maritime_transport/_delete_form.html.twig') }}
{% endblock %}
