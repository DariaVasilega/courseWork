{% extends 'base.html.twig' %}

{% block title %}RiverTransport{% endblock %}

{% block body %}
    <h1>RiverTransport</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ river_transport.id }}</td>
            </tr>
            <tr>
                <th>Engine</th>
                <td>{{ river_transport.engine }}</td>
            </tr>
            <tr>
                <th>Seats</th>
                <td>{{ river_transport.seats }}</td>
            </tr>
            <tr>
                <th>Weight</th>
                <td>{{ river_transport.weight }}</td>
            </tr>
            <tr>
                <th>Size</th>
                <td>{{ river_transport.size }}</td>
            </tr>
            <tr>
                <th>FuelCost</th>
                <td>{{ river_transport.fuelCost }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('river_transport_index') }}">back to list</a>

    <a href="{{ path('river_transport_edit', {'id': river_transport.id}) }}">edit</a>

    {{ include('river_transport/_delete_form.html.twig') }}
{% endblock %}
